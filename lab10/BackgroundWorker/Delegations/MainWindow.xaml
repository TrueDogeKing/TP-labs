<Window x:Class="Delegations.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Delegations"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Label Content="Number of Threads:" VerticalAlignment="Center"/>
            <TextBox Name="ThreadsTextBox" Width="50" Margin="5,0"/>
            <Button Name="RunButton" Content="Run Simulation" Width="120" Click="RunButton_Click" Margin="5,0"/>
            <Button Name="StopButton" Content="Stop" Width="80" Click="StopButton_Click" IsEnabled="False"/>
        </StackPanel>


        <ProgressBar Name="SimulationProgressBar" Grid.Row="1" Height="20" Minimum="0" Maximum="100" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Simulation Output:" FontWeight="Bold" Margin="0,10,0,5"/>

        <TextBox Name="OutputTextBox" Grid.Row="3" VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap" AcceptsReturn="True" IsReadOnly="True"/>
    </Grid>
</Window>
